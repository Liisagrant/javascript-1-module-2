const animal = ["cat", "dog", "cow"];
console.log(animal.length);

for (let i = 0; i < animal.length; i++) {
    console.log(animal[i]);
}

// trim
const name = "        Lisa   ";
console.log(name);

const trimmedName = name.trim();// remove space and make lisa look like "Lisa"
console.log(trimmedName);


